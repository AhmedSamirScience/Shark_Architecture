<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Shark_Architecture"
        tools:targetApi="31">
        <activity
            android:name=".presentation.activity.featureOptions.repeatCall.RepeatCallActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.activity.featureOptions.dataBaseRoom.DataBaseRoomActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.activity.featureOptions.apiCall.APICallActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.activity.featureOptions.MainFeatureOptionsActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.activity.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.activity.mainFeatures.MainFeaturesActivity"
            android:exported="false" />
    </application>

</manifest>